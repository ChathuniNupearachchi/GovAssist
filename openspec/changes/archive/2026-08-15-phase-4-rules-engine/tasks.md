## 1. Schema migration

- [x] 1.1 Add nullable `source_document_id` FK to `Requirement` and
      `FeeRule` in `api/app/models.py`
- [x] 1.2 Add `ResolutionNote` model (`id`, `code` unique, `note_text`,
      `primary_source_document_id` FK not null,
      `secondary_source_document_id` FK nullable, `created_at`)
- [x] 1.3 Generate and review the Alembic migration; fix any broken
      autogenerated import (per Phase 2's precedent) before applying
      — autogenerate emitted `create_foreign_key(None, ...)` /
      `drop_constraint(None, ...)`, invalid with no naming convention
      configured; named both constraints explicitly.
- [x] 1.4 `alembic upgrade head`; verify the two new columns and the new
      table exist — confirmed via inspector (16 tables incl.
      alembic_version, both FK columns present).
- [x] 1.5 `alembic downgrade -1`; verify the columns and table are gone
      and the other 13 Phase 2 tables are untouched; re-apply — confirmed
      15 tables remain after downgrade, columns gone; re-applied cleanly.

## 2. Renewal rule data — services, source documents, rule versions

- [x] 2.1 Write `api/app/seed/phase4_renewal.py` (or equivalent one-off
      script): resolve the `SourceDocument` rows for `pages_e.php?id=7`,
      `id=8`, `id=10` by `source_url` ordered by `fetched_at` ascending
      (first fetch, per design.md's stability decision)
- [x] 2.2 Create `Service(code="passport-renewal")` and
      `Service(code="passport-amendment")`
- [x] 2.3 Create the renewal `RuleVersion` (`source_document_id` = id=8,
      `status="draft"` initially, `verified_at` set)
- [x] 2.4 Create the amendment `RuleVersion` (`source_document_id` =
      id=10, `status="draft"` initially, `verified_at` set)

## 3. Renewal rule data — questions and conditions

- [x] 3.1 Create `Question` rows for: age, holds_passport, name_changed,
      dual_citizen, section_19_2, profession, buddhist_priest, district,
      service_basis — each with a `sequence`
- [x] 3.2 Create `Condition` rows for each attribute listed in design.md
      — implemented age eligibility (16-60) as two `lessThan` conditions
      (age<61, and age<16 negated) combined via two
      `REQUIREMENT_CONDITION` links rather than a single "between"
      operator, since only equals/lessThan/in exist; "profession stated"
      as `equals ""` negated (no not-empty operator exists either).
      `district` and `service_basis` deliberately have no Condition row
      — the office resolver reads `OFFICE.district` directly and the fee
      calculator compares `basis` directly, per design.md.

## 4. Renewal rule data — standard document set

- [x] 4.1 Photo studio acknowledgement (`kind=prerequisite`,
      unconditional, sequence before all `document` requirements,
      `source_document_id`=id=8)
- [x] 4.2 Birth certificate + photocopy, NIC + photocopy (unconditional,
      `kind=document`, id=8)
- [x] 4.3 Current passport + bio-data photocopy (`REQUIREMENT_CONDITION`
      → `holds_passport`, id=8)
- [x] 4.4 Marriage certificate + photocopy (`REQUIREMENT_CONDITION` →
      `name_changed`, id=8)
- [x] 4.5 Educational certificate + service document + photocopies
      (`REQUIREMENT_CONDITION` → `profession`, id=8)
- [x] 4.6 Samanera/Higher Ordination certificate + photocopies
      (`REQUIREMENT_CONDITION` → `buddhist_priest`, id=8)
- [x] 4.7 Link every standard-set requirement above (except the studio
      acknowledgement) to the `dual_citizen` condition, `negated=true`,
      per design.md's mutual-exclusion pattern

## 5. Renewal rule data — dual-citizen set and section 19(2)

- [x] 5.1 Completed application form, dual citizenship certificate +
      photocopy, foreign passport (+ any SL passport held) + bio-data
      photocopies, NIC + photocopy, birth certificate + photocopy —
      each linked to `dual_citizen` equals true (un-negated),
      `source_document_id`=id=8. Photo studio acknowledgement is not
      duplicated — the unconditional one from 4.1 already satisfies
      both source lists, which state the same "always" fact.
- [x] 5.2 New NIC prerequisite (`kind=prerequisite`, linked to
      `dual_citizen` equals true AND `section_19_2` equals true, id=8)

## 6. Renewal rule data — fingerprints, fees, offices, resolution note

- [x] 6.1 Fingerprints prerequisite (`kind=prerequisite`, linked to
      `age` in [16,60] range condition, `source_document_id`=id=7,
      label states in-person at Head Office or Regional Office only)
- [x] 6.2 Fee rules: `basis=normal` 10000.00, `basis=urgent` 20000.00,
      both `source_document_id`=id=8
- [x] 6.3 Seed `Office` rows if not already present from Phase 2 (Head
      Office, 5 Regional Offices already existed; added one `mission`
      office); confirmed no `type=ds` office is referenced anywhere in
      renewal data
- [x] 6.4 Seed the `ResolutionNote` (`code="urgent_office_conflict"`,
      primary=id=7, secondary=id=7, note text quoting both passages)

## 7. Renewal rule data — amendment service and approval

- [x] 7.1 Amendment fee rule (1200.00, id=10) — `basis="normal"` is a
      fixed value to satisfy the normal|urgent CHECK constraint; the
      source has no urgent/normal split for amendments (single flat fee)
- [x] 7.2 Amendment requirements: passport, marriage certificate
      (`kind=document`, id=10)
- [x] 7.3 Seed script asserts every expected `Condition.attribute` value
      exists (design.md Risk mitigation) before flipping status
- [x] 7.4 Set both rule versions' `status="approved"`, `approved_by` set

Verified directly against the seeded database: renewal rule version has
`status=approved`, cites id=8; every requirement/fee carries its own
`source_document_id` (id=7 for fingerprints, id=8 for the rest); dual-set
items are `dual_citizen=true`, standard-set items are `dual_citizen!=true`
— mutually exclusive by construction; amendment rule version is
independently `approved`, cites id=10, fee 1200.00; resolution note cites
id=7 twice; no `ds`-type office exists anywhere.

## 8. Engine — condition evaluator (4.1)

- [x] 8.1 `api/app/engine/conditions.py`: `evaluate(condition, negated,
      answers) -> bool` for `equals`, `lessThan`, `in` (comma-split)
- [x] 8.2 Missing-answer case returns not-satisfied, no exception —
      implemented so a missing answer's not-satisfied result is final
      and not flipped by `negated` (a genuine refinement over the
      spec's literal wording, needed so an unanswered `dual_citizen`
      can't prematurely satisfy a negated "NOT dual citizen" link;
      documented in `conditions.py`)
- [x] 8.3 Unit tests: one per operator, plus missing-answer

## 9. Engine — requirement resolver (4.2)

- [x] 9.1 `api/app/engine/requirements.py`: resolve requirements for a
      case's rule version, filtering on all linked conditions passing,
      ordered by `sequence`
- [x] 9.2 Unit tests: name-change vs. no-name-change sets differ;
      dual-citizen set excludes standard-set requirements; section
      19(2) includes the new-NIC prerequisite

## 10. Engine — fee calculator (4.3)

- [x] 10.1 `api/app/engine/fees.py`: resolve fee rule by service +
      basis answer
- [x] 10.2 Unit tests: normal and urgent, each with correct citation

## 11. Engine — office resolver (4.4)

- [x] 11.1 `api/app/engine/offices.py`: implement the precedence in
      design.md (full list → district narrowing → conflict-note
      attachment on urgent), fixed ordering
- [x] 11.2 Unit tests: same inputs → identical repeated output; urgent
      includes the conflict note; no `type=ds` ever returned

## 12. Engine — prerequisite ordering (4.5)

- [x] 12.1 Folded into `requirements.py` rather than a separate
      `prerequisites.py` (per this task's own "or fold into" option) —
      sequence is already the resolver's sort key and the seed data's
      sequence numbers already encode the required order
- [x] 12.2 Unit test: resolved plan's first item is the studio
      acknowledgement

## 13. Engine — next-question logic (4.6)

- [x] 13.1 `api/app/engine/next_question.py`: given answered questions,
      return the next relevant unanswered one (age first; skip
      section-19(2) follow-up when dual_citizen is false). Added
      `api/app/engine/renewal_intake.py` as a shared source of truth for
      the question<->attribute mapping, imported by both the seed
      script and this module, since `Question` itself carries no
      attribute column.
- [x] 13.2 Unit test: answering `dual_citizen=false` changes the next
      question returned vs. `dual_citizen=true`

## 14. Engine — scope gate and amendment branch

- [x] 14.1 Resolver entry point (`api/app/engine/resolver.py`)
      evaluates age first, unconditionally; under-16 short-circuits to
      the scope-gate response before any other resolution runs
- [x] 14.2 Name-changed-after-marriage surfaces the amendment
      alternative (fee, delivery time, documents) alongside the renewal
      resolution
- [x] 14.3 Unit tests for both behaviors

## 15. Golden test set (4.7)

- [x] 15.1 `api/tests/engine/golden_scenarios.py`: the ten scenarios
      from BACKEND_PLAN.md Phase 4.7 as data fixtures with hand-verified
      expected requirements/fee/offices/flags. Two interpretation notes
      recorded in the file: scenario 4 ("expired over 5 years ago") is
      modeled as `holds_passport=false` (an expired passport is not a
      *valid* one to submit); scenario 9 ("applying from abroad") is
      modeled as `district=None`, since Phase 4 does not build
      overseas-specific office routing (id=9 is out of this phase's
      scope) — the office resolver's documented behavior for an unknown
      district is "list every Regional Office."
- [x] 15.2 `api/tests/engine/test_golden.py`: runs the real resolver
      against every fixture and asserts the expected output — 37/37
      tests pass (10 golden + 27 component unit tests)
- [x] 15.3 Verified the suite fails on a deliberately introduced
      regression: changed the seeded normal fee to 9999.00, re-seeded,
      re-ran — 9 of 37 tests failed (all golden scenarios asserting the
      normal fee, plus the direct fee test). Reverted and re-verified
      37/37 pass again. No CI workflow file exists in this repo yet
      (Phase 1 didn't set one up); documented the command CI must run:
      `cd api && pytest tests/` (after `python -m app.seed.phase4_renewal`
      against the target database).

## 16. Verification against Done When

- [x] 16.1 Walked every "Done when" criterion in the Phase 4 request
      against the golden suite and unit tests — see the apply summary
      for the full mapping; every criterion is covered by at least one
      passing test.
